#include <fstream>
#include <string>
#include <iostream>
using namespace std;

int main()
{
  char ch[80];
  string x = "abcde";
  for(int i = 0; i < x.length(); i++)
  {
    ch[i] = x[i];
    cout << ch[i] << endl;
  }
  return 0;
}
